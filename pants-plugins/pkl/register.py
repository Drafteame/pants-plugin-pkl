# Stub register.py — will be fully implemented in T04.
# Must exist so the `pkl` backend can be loaded via pants.toml backend_packages.


def target_types():
    return []


def rules():
    return []
